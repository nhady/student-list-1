node{
  def app
  
  stage('Clone') {
    checkout scm
  }
  
  stage('Build image') {
    app = docker.build("my_image", "./simple_api/")
  }
  
  stage('Test image') {c ->
      sh 'docker ps'
    app.inside{
    echo'Test "test passed"'}
      
    }
  }
