node{
  def app
  
  stage('Clone') {
    checkout scm
  }
  
  stage('Build image') {
    app = docker.build("4dvop-image", "./simple_api/")
  }
  
  stage('Test image') {
    docker.image('my_image').withRun("--name mycont -p 5000:5000 -e DEBUG=true"){c ->
      sh 'docker ps'
      sh 'docker inspect mycont'
      sh'curl -u "toto:python" -X GET http://0.0.0.0:5000/pozos/api/v1.0/get_student_ages'
    
    }
  }
}
